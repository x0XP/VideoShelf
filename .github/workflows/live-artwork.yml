name: Live artwork regression

on:
  push:
    branches: [ main ]
    paths:
      - 'src/VideoShelf/**'
      - 'Start.cmd'
      - '.github/workflows/live-artwork.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'src/VideoShelf/**'
      - 'Start.cmd'
      - '.github/workflows/live-artwork.yml'

jobs:
  artwork:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - name: Build VideoShelf
        shell: cmd
        run: Start.cmd --build-only

      - name: Require a live artwork image
        shell: powershell
        run: |
          $queries = @('Re Zero anime','Windows 11 wallpaper','Big Buck Bunny')
          $success = $false
          foreach ($query in $queries) {
            Write-Host "Testing live artwork for: $query"
            $process = Start-Process -FilePath '.\VideoShelf.exe' -ArgumentList @('--test-online-artwork',('"' + $query + '"')) -Wait -PassThru -NoNewWindow
            if ($process.ExitCode -eq 0) {
              $success = $true
              break
            }
            Start-Sleep -Seconds 2
          }
          if (-not $success) {
            throw 'All live artwork lookups failed. VideoShelf would display blank online artwork.'
          }

      - name: Require real artwork to render in the UI
        shell: powershell
        run: |
          $output = Join-Path $env:RUNNER_TEMP 'VideoShelf-live-artwork.png'
          $process = Start-Process -FilePath '.\VideoShelf.exe' -ArgumentList @('--screenshot-rezero',('"' + $output + '"')) -PassThru -NoNewWindow
          if (-not $process.WaitForExit(90000)) {
            $process.Kill(); $process.WaitForExit()
            throw 'Live artwork UI render exceeded 90 seconds.'
          }
          if ($process.ExitCode -ne 0) { throw "Live artwork UI render failed with exit code $($process.ExitCode)." }
          if (!(Test-Path $output) -or (Get-Item $output).Length -lt 5000) {
            throw 'Live artwork UI render did not create a valid screenshot.'
          }
          Copy-Item $output '.\VideoShelf-live-artwork.png'

      - uses: actions/upload-artifact@v4
        with:
          name: VideoShelf-live-artwork-proof
          path: VideoShelf-live-artwork.png
          if-no-files-found: error
